
  # After registering a name on build.snapcraft.io, commit an uncommented line:
  # name: your-registered-name
  version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
  summary: a 
  description:JSpdf powered call sheet generator for photo shoots/productions |
    a JSpdf powered call sheet generator for photo shoots/productions to get everything organized into a handy PDF for email to PA and the rest of production

  grade: dev # must be 'stable' to release into candidate/stable channels
  confinement: devmode # use 'strict' once you have the right plugs and slots

  parts:
    my-part:
      # See 'snapcraft plugins'
      plugin: nil
  
